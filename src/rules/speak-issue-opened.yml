---
conditions:
  all:
    # This is an Issue !
    - fact: payload.issue.id
      operator: notEmpty
      value: ''
    # This Issue was opened !
    - fact: payload.action
      operator: equal
      value: opened
    # This NOT an Issue comment !
    - fact: payload.comment.id
      operator: isEmpty
      value: ''

event:
  type: issueHandler
  params:
    data: 
      # some greeting string
      greeting: 'hello {{user}}, there is a new Issue. '
      # what Issue info do you want to hear about ? (true/false)
      issue_action: false
      issue_user: true
      issue_number: false
      issue_title: false
      issue_repository: true
      issue_body: true
      issue_comment: false

# default is '1'
priority: 1
