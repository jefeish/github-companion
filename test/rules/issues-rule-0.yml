---
conditions:
  all:
    - fact: payload.issue.user.login
      operator: equal
      value: jefeish
    # Prevent loops
    - fact: payload.sender.type
      operator: notEqual
      value: Bot
event:
  type: createComment-by-rule
  params:
    data: Let's play by the Rules!
